extends layout

block content
  if user
    h1 Hello User: #{user.email}
    form(action="create-room", method="post")
      input(type="text", name="name")
      button(type="submit") Create Room!
  else
    h1 Please login!